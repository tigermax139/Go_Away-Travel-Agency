<!DOCTYPE html>
<html lang="ru">
<head>
    <!--favicon-->
    <link rel="apple-touch-icon" sizes="180x180" href="../img/favicons/apple-touch-icon.png">
    <link rel="icon" type="image/png" sizes="32x32" href="../img/favicons/favicon-32x32.png">
    <link rel="icon" type="image/png" sizes="16x16" href="../img/favicons/favicon-16x16.png">
    <link rel="manifest" href="../img/favicons/manifest.json">
    <link rel="mask-icon" href="../img/favicons/safari-pinned-tab.svg" color="#5bbad5">
    <meta name="theme-color" content="#ffffff">
    <!--meta tag-->
    <meta charset="utf-8">
    <meta http-equiv="X-UA-Compatible" content="IE-edge">
    <meta name="viewport" content="width=device-width">
    <!--style-->
    <link rel="stylesheet" href="../style/fonts.css">
    <link rel="stylesheet" href="../style/general.css">
    <link rel="stylesheet" href="../style/sprites.css">
    <link rel="stylesheet" href="../style/media.css">
    <title>Go Away Travel Agency</title>
</head>
<body>
<h1>Ваш заказ</h1>
<div class="ticket">
    <div class="ticket__container">
        <div class="ticket__top">
            <img src="../img/logo/logo-white.png" alt="Go Away Travel Company Logo">
            <div class="ticket__title">
                <h2 class="section__description">Путевка к хорошему настроению!</h2>
                <% oneOrder.forEach((order, index) => { %>
                <span class="ticket__id"><%= order._id %></span>
            </div>
        </div>
        <div class="ticket__content">
            <div class="ticket__items">
                <p class="ticket__item">
                    <span class="ticket__item-static">Страна</span>
                    <span class="ticket__item-generate"><%= order.orderCountry %></span>
                </p>
                <p class="ticket__item">
                    <span class="ticket__item-static">Отель</span>
                    <span class="ticket__item-generate"><%= order.orderHotel %></span>
                </p>
                <p class="ticket__item">
                    <span class="ticket__item-static">Тип Комнаты</span>
                    <span class="ticket__item-generate"><%= order.orderRoom %> </span>
                </p>
                <p class="ticket__item">
                    <span class="ticket__item-static">Дата Заезда</span>
                    <span class="ticket__item-generate"><%= order.orderDateIn %></span>
                </p>
                <p class="ticket__item">
                    <span class="ticket__item-static">Дата Отьезда</span>
                    <span class="ticket__item-generate"><%= order.orderDateOut %></span>
                </p>
                <p class="ticket__item">
                    <span class="ticket__item-static">ФИО</span>
                    <span class="ticket__item-generate"><%= order.name %></span>
                </p>
                <p class="ticket__item">
                    <span class="ticket__item-static">Номер телефона</span>
                    <span class="ticket__item-generate"><%= order.number %></span>
                </p>
                <p class="ticket__item">
                    <span class="ticket__item-static">Стоимость</span>
                    <span class="ticket__item-generate ticket__price"><%= order.orderPrice %></span>
                </p>
            </div>
            <div class="ticket__img">
                <div class="qrcode" id="qr">

                </div>
            </div>
        </div>
    </div>
</div>
<div class="ticket__btn">
    <a href="/delete/<%= order._id %>"> Нашел ошыбку, заказать снова</a>

    <button type="button" onclick="window.print()">печать билета</button>
</div>
<script src="../js/qrcode.min.js"></script>
<script src="../js/jquery.min.js"></script>
<script>
    const data = 'http://localhost:3000/ticket/<%= order._id %>';
    <% }); %>
    const qrcode = new QRCode(document.getElementById("qr"));
    qrcode.makeCode(data);
</script>
</body>
</html>